#ifndef INPOST_H
#define INPOST_H
#include <string>
#include <iostream>
#include "mystack.h"


std::string convert(const std::string&);
int precedence (const char);
bool isoperator (const char);

#endif
